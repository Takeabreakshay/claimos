# ASSUMPTIONS.md — ClaimOS

Every number in this project is either **[SOURCED]** (from IRDAI AR 2024-25 / 2024 Master Circular /
IIB / PwC — see `CLAUDE.md §2`, locked) or **[ASSUMPTION]** (a modeling calibration). This file logs
**every assumption**, so nothing is silently invented. When you add or change an assumption, add a row
here — same day, with a one-line rationale. Do not introduce a number anywhere in the code that is not
either sourced or logged below.

> Rule of thumb: if a reviewer asks "where did this figure come from?", the answer must be either a §2
> citation or a row in this table. No third option.

---

## Log

| Date | Parameter (file) | Value | Rationale | Status |
|------|------------------|-------|-----------|--------|
| 2026-07-19 | `n_claims` (distributions) | 50,000 | Large enough for stable model training + calibration; small enough to run fast locally. | active |
| 2026-07-19 | `claim_type_mix` | OD 0.75 · TP 0.18 · theft_total 0.07 | OD dominates Indian motor by claim count; TP a meaningful minority; theft/total rare. Directional, consistent with §2 (motor is largest claims outlay, OD+TP). | active |
| 2026-07-19 | `claim_amount.OD` | lognormal μ=ln(18000), σ=0.90 | Median ≈ ₹18k so the majority of OD claims fall under the ₹50k surveyor seam (small dents dominate). Tuned to hit the `sub_50k_share` band. | active |
| 2026-07-19 | `claim_amount.TP` | lognormal μ=ln(85000), σ=1.00 | TP (injury/property) typically exceeds the ₹50k seam. | active |
| 2026-07-19 | `idv` | lognormal μ=ln(450000), σ=0.50 | Mid-market Indian car IDV; drives theft_total settlement. | active |
| 2026-07-19 | `acceptance.sub_50k_share` | [0.55, 0.68] | Guardrail band asserted in `test_data_gen`; keeps the touchless envelope realistic and defensible. Anchored to the §2 ₹50k seam. | active |
| 2026-07-19 | `fraud_base_rate` | OD 0.08 · TP 0.20 · theft_total 0.15 | Blends to ~10–12% overall, inside the §2 "10–15% of claims" range; TP weighted higher per §2 ">50% of TP alleged bogus" (alleged ≠ confirmed, so confirmed rate set well below). | active |
| 2026-07-19 | `fraud_signal_lift` | non_network 0.55 · late_intimation 0.40 · undeclared_mod 0.35 · photo_reuse 0.30 | Correlated-but-noisy signals so fraud is learnable (target AUC ~0.80–0.90), not trivially separable. | active |
| 2026-07-19 | `severity_mix` | minor 0.55 · moderate 0.30 · severe 0.15 | Minor damage dominates; theft_total is always `total`. | active |
| 2026-07-19 | `policy_status` | active 0.93 · lapsed 0.07 | Small lapsed slice to exercise the coverage-reject path. | active |
| 2026-07-19 | `garage_type` | network 0.65 · non_network 0.35 | Non-network share drives the reimbursement route (+15–30d TAT, which IS §2-sourced). | active |
| 2026-07-19 | `intimation.within_48h_share` | 0.85 | Most claims reported promptly; the 15% late tail exercises the "delayed intimation" trap. | active |
| 2026-07-19 | `intimation.late_reason_valid_share` | 0.40 | Of late claims, share with a legally valid reason. Behaviour (late-alone ≠ valid reject) is §2-sourced (SC rulings); the 0.40 split is the assumption. | active |
| 2026-07-19 | `driver.valid_license` / `dui_flag` | 0.95 / 0.02 | Small invalid-licence / DUI slices to exercise eligibility flags. | active |
| 2026-07-19 | `modification.actual_rate` / `declared_given_actual` | 0.10 / 0.40 | Creates the undeclared-modification subset (a fraud/eligibility signal). | active |
| 2026-07-19 | `photo.quality_beta` / `num_photos` | Beta(5,2) / [2,8] | Skews photo quality toward "usable"; photo count range for capture realism. | active |
| 2026-07-19 | `vehicle_age_years` | 0–15, right-skewed | Typical insured-fleet age spread. | active |
| 2026-07-19 | `geo` | metro 0.40 · urban 0.40 · rural 0.20 | Directional geographic spread. | active |
| 2026-07-19 | `escalation.base_rate` | 0.07 | Jumper/sleeper share. Window (90d) IS §2-sourced; the 7% base rate is the assumption. Target escalation AUC ~0.70 by design (hard to see at FNOL). | active |
| 2026-07-19 | `rings.*` | 30–50 rings · 5–20 claims each | Seeds collusion structure for the graph model; target ≥80% ring recovery. | active |
| 2026-07-19 | `claim_amount.OD.mu` (distributions) | **10.05** (was 9.80) | **CHANGE (Phase 1):** at μ=9.80 the generated `sub_50k_share` was 0.7043, above the 0.68 ceiling. Nudged OD median ₹18k→~₹23k per the §5.1 / LOGIC §1.1 acceptance protocol; new share = 0.6547 (mid-band). ₹50k anchor untouched. | active |
| 2026-07-19 | `fraud_signal_base.*` (distributions) | non_network 0.35 · gt_48h 0.15 · photo_reuse 0.05 · mod_actual 0.10 | q_s = P(signal=1 \| not fraud) baselines for the LOGIC §1.3 lift formula. Set to the existing marginals; fraud lifts each upward, which is the learnable correlation. | active |
| 2026-07-19 | `cost.severity_factor` | minor 0.03 · moderate 0.09 · severe 0.22 · total 0.95 | Fraction of IDV → base_cost (LOGIC §1.4, explicitly flagged "assumption, log it"). Drives the cost-model label. | active |
| 2026-07-19 | `cost.{base_noise, genuine_lognorm_sigma, inflated_claim_mult}` | U(0.9,1.1) · σ=0.12 · U(1.3,2.5) | Noise on base_cost; genuine claim≈true+mild noise; staged/inflated fraud overwrites claim to 1.3–2.5× true → the claim-vs-true gap fraud feature. | active |
| 2026-07-19 | `escalation_model.*` | hidden rates 0.15/0.12/0.10 · βs (1.1,0.9,1.3,0.5,0.4) · noise σ=0.5 | Latent logistic for `escalated_at_90d` (LOGIC §1.5). Intercept auto-tuned by bisection so mean = `escalation.base_rate`; σ=0.5 keeps it hard-at-FNOL (AUC ~0.70). | active |
| 2026-07-19 | `surveyor.*` | fraud_reject 0.60 · partial_base 0.05 +0.02/yr (≤0.45) · dep 0.04/yr (≤0.50) · override 0.10 | Surveyor verdict / settlement / override labels (LOGIC §1.6). Surveyor catches most (not all) fraud; partials & depreciation rise with vehicle age. | active |
| 2026-07-19 | `entities.*` | genuine=unique entities · ring shares 1–2 garages · 0.50 bank-share | Non-ring claims get UNIQUE garage/surveyor/bank/phone (sparse linkage per LOGIC §1.7) so the graph cleanly isolates seeded rings; rings reuse 1–2 garages + one surveyor. (Superseded the earlier garage/surveyor pool idea, which created spurious legit components.) | active |
| 2026-07-19 | `graph.*` | link=[garage,surveyor,bank,phone] · ring_min_size 5 · λ=0.3 | Collusion-graph params (LOGIC §3.5): components ≥5 flagged as rings; `ring_risk = 1 − exp(−λ(size−1))`. | active |
| 2026-07-19 | `model_params.*` | LGBM: lr 0.05 · leaves 31 · min_child 40 · subsample/colsample 0.8 · 400 trees · seed 42 · test 0.25 · calib 0.20 | LightGBM hyperparameters and split sizes for the Phase-3 models (GBT-only per §11). Right-sized defaults; tune if a §7 target is missed. | active |
| 2026-07-19 | `fraud_signal_lift` (distributions) | non_network 0.35 · gt_48h 0.30 · mod 0.30 · photo_reuse 0.20 | **CHANGE (Phase 3):** reduced from 0.55/0.40/0.35/0.30 so trained fraud ROC-AUC lands **0.869** — inside the 0.80–0.90 "learnable-not-trivial" band (§5.2). At the original lifts (plus the rails) AUC was ~0.94. | active |
| 2026-07-19 | `escalation_model.beta` (distributions) | tp 0.3 · amb 0.3 · injury 0.4 · low_amount 0.8 · low_sev 0.7 · **claim_type_tp 2.4** | **CHANGE (Phase 3):** rebalanced toward FNOL-visible drivers (added `claim_type_tp`; the 3 hidden latents stay excluded from the classifier per §3.3) so escalation ROC-AUC reaches **0.735** (>0.70) vs 0.54 when hidden signals dominated. | active |
| 2026-07-19 | rails mock correlations (`rails.py`) | PRISM logit 0.7·fraud+0.7·ring+1.5·Z · eng/chassis doc-match 0.90 · prior-flag λ 0.06+0.07·fraud+0.4·ring | Mock-rail signal strengths, softened so the rails inform fraud without making it trivial. Mock-behaviour params (not statistical claims); documented inline with `# PRODUCTION:` swap points. | active |
| 2026-07-21 | **Leakage measurement basis** (`evaluate.py`) | **held-out test rows only** (was whole book) | **CRITICAL CORRECTION.** Lane-1 leakage was being measured across all 50k claims — but ~75% of those rows were used to fit the models, where fraud predictions are optimistically good. Measured split (`reports/leakage_split.txt`): whole book **1.195%** · train rows 0.373% · **held-out 2.425%**. The shipped gate was therefore **breaching the 1.5% ceiling out-of-sample** while the guardrail reported "OK" and never fired. Leakage is now governed on held-out rows only; the whole-book figure is retained in `eval.json` as `lane1_leakage_whole_book_biased` for contrast and must never be quoted. | active |
| 2026-07-21 | Auto-tighten algorithm (`evaluate.py`) | envelope search (max touchless s.t. leakage ≤ 0.8×ceiling) | The old loop ratcheted `min_confidence` +0.02 and `max_fraud_prob` −0.01 together each step; against honest leakage it overshot to **0.1% touchless**. Replaced with the L7 §6 envelope search run in the tightening direction. Result: `min_confidence` 0.85 (unchanged), `max_fraud_prob` **0.01**, held-out leakage **0.56%**, touchless **5.6%**. | active |
| 2026-07-20 | `lane1_touchless.max_fraud_prob` (thresholds) | **0.07** (was 0.10) | **CHANGE:** adopted the Bajaj feature brief's tighter gate. Swept 0.10→0.05 on the full 50k book (`reports/threshold_sweep.json`): touchless **25.11%** and leakage **1.195%** are *identical* across 0.06–0.10 (calibrated fraud scores are bimodal — nothing sits in that band), so the tighter posture is free. **Key finding:** narrowing the gate does NOT cut leakage — the leaking frauds score below 0.06, i.e. the model is confidently wrong on them. Leakage is a detection problem, not a threshold problem. | active |
| 2026-07-19 | `model_params.cost_band_penalty_weight` | 0.30 | Confidence combination (CLAUDE §7): `model_confidence = min(c_fraud,c_esc) · (1 − 0.30·cost_band)`. Cost-band uncertainty is a PENALTY, not a co-equal `min` term — the equal-min reading starved Lane 1 (5.8% touchless). With the penalty, touchless rises to 25.1% and Lane-1 leakage stays 1.20% < 1.5% (auto-tighten still binds). | active |
| 2026-07-19 | `tat_sim.*` | report Tri(2,8,15) · decision U(1,7) · litigation LogN(μ4.607,σ0.60)≈120d · dispute 0.50 | Baseline all-manual TAT draws (LOGIC §1.8). Survey-15d / decision-7d bounds reuse the SOURCED `tat_ceilings`; the litigation tail is the 3–12+ month rejection tail. | active |
| 2026-07-22 | `duplicate_detection` (thresholds) | window_days 30 · amount_tolerance 0.20 | **NEW (PS6 deliverable #2, live layer).** A new claim on the same `policy_id`/`customer_id` whose amount is within ±20% of a prior claim and whose incident falls within 30 days is treated as a duplicate and force-routed to Lane 3 investigative regardless of value. Window/tolerance are operational assumptions (no §2 figure exists for duplicate spacing); chosen to catch same-event re-filing without flagging legitimate repeat claims months apart. | active |
| 2026-07-22 | Vision damage read (`live/nvidia.py`, `live/workflow.py`) | severity_from_photo gated at quality ≥ 0.45 & not blurry · worst-of aggregation | **NEW (PS6 deliverable #3, live layer).** A vision model (`NVIDIA_VLM_MODEL`, default `meta/llama-3.2-11b-vision-instruct`) reads {severity, damaged_parts, confidence} from each photo. Assessment is attempted only on a usable frame (assessing blur is worse than abstaining). Claim-level `cv_severity` = worst across photos (`_SEV_RANK` minor<moderate<severe<total); `cv_severity_mismatch` set when it differs from the operator-declared severity. Superseded 2026-07-23: the read is now a HARD input (see damage-mismatch below), not advisory. Durable persistence needs `supabase/migration_002_cv_damage.sql`. | active |
| 2026-07-23 | **Parts + labour rate card** (`config/rate_card.yaml`, `src/rate_card.py`) | segment mults hatch 1.0…luxury 4.5 · per-part price/labour table · labour ₹200–2000/hr by class×tier · consumables 5–10% · uncertainty band=0.15+0.05·n+0.10·struct | **NEW (LOGIC §1, user-supplied).** Deterministic line-item repair estimator → P10/P50/P90. `[SOURCED]`: paint 50% material dep, consumables 5–10%. `[DEFAULT]`: all prices/multipliers/labour (Indian aftermarket brief; tune on Bajaj data). Stacks with the GBT as a strong cost prior; `claim ÷ line-item` is a padding signal (Lane 2 at >1.3×, Lane 3 at >1.8×). Airbag/structural = hard escalators (never touchless); engine/gearbox = total-loss trigger. | active |
| 2026-07-23 | **Coverage matrix** (`config/coverage.yaml`, `models/coverage.py` v2) | compulsory excess ₹1000/2000 (PC by cc) · NCB 20→50% · zero-dep ≤5yr cap 2/yr · engine-protect ≤7yr · RTI ≤3yr · total-loss at 75% IDV · owner PA ₹15L | **NEW (LOGIC §2, user-supplied).** Live 4-state decision (CLEAR/FLAG/HARD_DECLINE/LEGAL_WEAK) + settlement waterfall + advise-withdraw (deductible & NCB break-even). `[SOURCED]`: excess grid, NCB slab, PA ₹15L, 75% total-loss. `[DEFAULT]`: add-on ages/caps, 15% invoice-variance. New checks: policy-in-force **on incident date** (not filing), cover-type vs claim-type, usage class, engine-peril exclusion. Legal-weak (late-but-valid, no-nexus breach) never auto-declines. v2 is authoritative in the live path; the batch coverage (eval) is unchanged. Persistence: `migration_003`. | active |
| 2026-07-23 | **Duplicate / re-filing** (`config/thresholds.yaml`, `live/workflow.py`) | T1 exact→L3 · T2 near (7d, Jaccard≥0.60, ±25%)→L3 · T3 re-file-after-reject (90d)→L2 · T4 same-part (180d)→L2 · freq ≥3/12m flag, ≥2/30d→L3 · near-inception 15d / expiry 7d backdating | **CHANGE (LOGIC §3, replaces coarse 30d/±20%).** Four-tier rule with Jaccard parts-overlap; frequency-anomaly + backdating tells. All `[DEFAULT]`, tune on Bajaj data. Cross-insurer duplicate (same chassis at another insurer) noted as the IIB-integration ask (not computable locally). | active |
| 2026-07-23 | **Damage-mismatch action policy** (`config/thresholds.yaml`, `live/workflow.py`) | delta=vision−declared on minor<mod<severe<total: +2→L3 · +1→L2 (silent only if conf<0.75) · −1→L2 (inflation) · −2→L3 + p_fraud+0.15 | **NEW (LOGIC §4, user-supplied).** HARD rules, not advisory (advisory mismatch is worthless). `[DEFAULT]` fraud bump 0.15, silent-tolerance conf 0.75. | active |
| 2026-07-23 | **Vision confidence floor** (`config/thresholds.yaml`, `live/workflow.py`) | value-tiered: <₹25k floor 0.80 · ₹25–50k floor 0.88 · >₹50k never touchless · +0.05 for non-network · capture<0.60 retake · EXIF staleness 72h | **NEW (LOGIC §5, user-supplied).** Below the floor for the claim's value band, the damage read can't support Touchless → Lane 2. All `[DEFAULT]`. | active |
| 2026-07-23 | `model_params.escalation_in_confidence` (distributions) | **false** (was implicitly true) | **CHANGE (LOGIC §7 retune).** `model_confidence` was `min(c_fraud, c_esc)·penalty`, but escalation RISK is already gated by `lane1.max_escalation_prob`. Keeping c_esc in the min double-counted escalation: a claim the escalation model was merely UNSURE about (p_esc≈0.5 → c_esc≈0) had its confidence crushed even with confidently-low fraud, capping touchless at ~5%. Now `model_confidence = c_fraud·penalty`; escalation risk stays governed by its own gate. Fraud is the auto-clear safety signal (leakage = auto-clearing a fraud). | active |
| 2026-07-23 | `lane1_touchless.min_confidence` (thresholds) | **0.80** (was 0.85) | **CHANGE (LOGIC §7 retune).** The 0.85 floor was binding on nearly the whole eligible book (56% of claims pass the amount+severity gates; sub-50k share 0.655, mid-band — so the denominator was fine, the floor was the cap). Envelope backtest confirms 0.80 is safe paired with the fraud gate. The auto-tighten still governs held-out leakage under the ceiling. | active |
| 2026-07-23 | **Customer UX layer** (`web/claim.*`, `src/live/policy.py`, `/claim` route) | mocked policy master · deterministic vehicle/coverage profile per policy number | **NEW (PS6 deliverable #7).** Mobile-first self-service claim journey (policy lookup → incident → live photo vision → instant triage → advise-withdraw), running on the same live engine as the insurer console. `lookup_policy()` is a MOCKED core-policy-DB rail (deterministic from the policy number, no RNG) supplying vehicle + coverage attributes so the matrix/rate-card have real inputs. `[DEFAULT]` demo fleet/add-on/NCB distributions; **PRODUCTION: replace with Bajaj's core policy DB read.** | active |
| 2026-07-23 | Auto-tighten margin (`evaluate.py`) | **0.95×ceiling** (was 0.8×); search down to conf 0.80 | **CHANGE (LOGIC §7 retune).** The 0.8×ceiling margin left ⅓ of the safety budget unused (touchless stuck ~5%). Now uses most of the budget deliberately (edge-of-ceiling posture) and searches conf downward from the floor, since the floor itself may be over-tight. **Result (held-out): touchless 4.4% → 16.1%, leakage 0.79% → 1.347% (< 1.5% ceiling), cost MAPE 10.4%, settled ≤7d 2% → 48%.** Honest envelope peaks at 18.5% touchless at the 1.5% ceiling. NOTE: 25–40% touchless is NOT reachable at ≤1.5% leakage on this synthetic data — the fraud model's held-out leakage curve is too steep (leakage is a detection problem, not a threshold problem); closing that gap needs better fraud detection or real labelled claims, and must never come from breaching the hard ceiling (Rule 8). | active |

---

## Model-target metrics (assumed acceptance thresholds — see `CLAUDE.md §7/§9`)

| Metric | Threshold | Rationale | Status |
|--------|-----------|-----------|--------|
| Fraud ROC-AUC | > 0.80 | Credible discrimination without being fake-perfect. | active |
| Escalation ROC-AUC | > 0.70 | Latent-by-design; a lower bar is honest. | active |
| Cost MAPE | < 25% | Reasonable for a synthetic-data cost estimator. | active |
| Calibration Brier | < 0.15 | Confidence must be trustworthy before routing. | active |
| **Lane-1 leakage** | **< 1.5% (HARD)** | The make-or-break metric; not an assumption to relax — it is a ceiling. | locked |

---

## Change protocol
1. Never change a **[SOURCED]** value. If §2 itself needs updating, that's a research task, flagged separately.
2. To change an **[ASSUMPTION]**: update the value in `config/*.yaml`, add/append a row here (new date, rationale), and re-run the affected phase's acceptance test.
3. If a change moves a headline number in the deck (e.g. % touchless, TAT drop), note it so the pitch and the model stay in sync.
